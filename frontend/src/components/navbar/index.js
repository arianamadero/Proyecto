import React from 'react';

const NavBar = () => (
    <nav>
        <div className="nav-wrapper grey darken-4">
        <a href="/" className="brand-logo center">Registro de empleados</a>
        </div>
    </nav>
);

export default NavBar;